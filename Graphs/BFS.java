package Graphs;

import java.util.*;

public class BFS {

    public static void main(String[] args) {
        ArrayList<Node> graph = new ArrayList<>();
        Node node = new Node(0);
        graph.add(node);
    }

}
